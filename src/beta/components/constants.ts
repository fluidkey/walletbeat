/** Non-expanded row height. */
export const shortRowHeight = 56;

/** Expanded row height. */
export const expandedRowHeight = shortRowHeight * 3;

/** Width of rating cells. */
export const ratingCellWidth = shortRowHeight * 2 + 32;
